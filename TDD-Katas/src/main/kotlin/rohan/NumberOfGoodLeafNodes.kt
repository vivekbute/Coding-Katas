package org.example.rohan

import javax.swing.tree.TreeNode

class NumberOfGoodLeafNodes {
    fun countPairs(root: TreeNode?, distance: Int): Int {
        val leaves = mutableListOf<TreeNode>()
        val count = 0

        return count
    }
}
